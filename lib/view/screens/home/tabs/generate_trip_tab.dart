import 'package:flutter/material.dart';

class GenerateTripTab extends StatelessWidget {
  const GenerateTripTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(color: Colors.black54,);
  }
}
