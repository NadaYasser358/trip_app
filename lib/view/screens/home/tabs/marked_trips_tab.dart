import 'package:flutter/material.dart';

class MarkedTripsTab extends StatelessWidget {
  const MarkedTripsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
