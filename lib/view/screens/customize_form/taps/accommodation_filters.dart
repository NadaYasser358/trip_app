import 'package:flutter/material.dart';

class AccommodationFilter extends StatelessWidget {
  const AccommodationFilter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(color: Colors.black,);
  }
}